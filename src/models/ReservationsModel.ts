export interface ReservationsModel {
    _id: string
    table_id: string,
    name: string,
    email: string,
    phone: number,
    reservation_time: string,
    createdAt: string
    updatedAt: string
    __v: number
}